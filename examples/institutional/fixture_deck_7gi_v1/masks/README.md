Static mask placeholders for rendered parity comparison.

The weekend vertical-slice implementation uses the fixture slide order as the
page alignment source of truth. If parity masking is needed, add PNG or JSON
mask definitions here keyed by fixture slide number.
