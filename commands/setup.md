---
description: >
  Check Inkline installation, typst, bridge health, and available
  brands. Run this first if you encounter any errors.
user-invocable: true
allowed-tools:
  - Bash(curl *)
  - Bash(python3 *)
  - Bash(pip *)
  - Bash(which *)
  - Bash(inkline *)
---

# /inkline:setup — Environment Check and Setup Guide

Use the `setup` skill to check and configure the Inkline environment.
